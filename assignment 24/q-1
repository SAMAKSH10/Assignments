#include <iostream>
using namespace std;
void prime(int n){
    int i;
    for(i=2;i<=n;i++){
    if(n%i==0 && i==n){
       cout<<"Number is prime"<<endl; 
       break;
    } 
     else if(n%i==0 && i!=n) {
        cout<<"Number is not prime"<<endl; 
        break;
     }
     
    }
} 

int main()
{
   int n;
   cin>>n;
   prime(n);
    return 0;
}
